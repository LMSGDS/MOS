# Place CI artifacts here on the school server so /cai-dat can serve them:
#   MOS-Dock-Setup-Windows.exe
#   MOS-Dock-Setup-macOS.pkg
#   MOS-Dock-Setup-macOS.zip  (fallback if .pkg is not built yet)
